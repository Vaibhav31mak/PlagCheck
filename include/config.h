
#include <iostream>
#include <string>
#include <map>

using namespace std;

extern int THRESHOLD;
extern int WINDOW_SIZE;
extern int GRAMS;
extern int PRIME;
extern int CODEBLOCK_SIZE;
extern string SHEETNAME;


extern map<string, string> diff;  // the diff of the two codes in the pair SubmissionId1_SubmissionId2
extern map<string, string> similar; 